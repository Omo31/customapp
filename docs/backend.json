
{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "Represents a user's profile in the system.",
      "type": "object",
      "properties": {
        "firstName": {
          "type": "string",
          "description": "The user's first name."
        },
        "lastName": {
          "type": "string",
          "description": "The user's last name."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The user's email address."
        },
        "roles": {
          "type": "array",
          "description": "A list of roles assigned to the user.",
          "items": {
            "type": "string",
            "enum": ["dashboard", "orders", "quotes", "users", "purchase-orders", "accounting", "analytics", "notifications", "settings"]
          }
        },
        "createdAt": {
            "type": "string",
            "format": "date-time",
            "description": "The timestamp when the user profile was created."
        }
      },
      "required": ["firstName", "lastName", "email", "roles", "createdAt"]
    },
    "Quote": {
      "title": "Quote",
      "description": "Represents a customer's request for a quote on custom items.",
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The ID of the user who created the quote."
        },
        "customerName": {
          "type": "string"
        },
        "customerEmail": {
          "type": "string",
          "format": "email"
        },
        "customerPhone": {
          "type": "string"
        },
        "items": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "name": { "type": "string" },
              "quantity": { "type": "string" },
              "unit": { "type": "string" },
              "customUnit": { "type": "string" },
              "unitCost": { "type": "number" }
            },
            "required": ["name", "quantity", "unit"]
          }
        },
        "services": {
          "type": "array",
          "items": { "type": "string" }
        },
        "additionalNotes": { "type": "string" },
        "deliveryOption": {
          "type": "string",
          "enum": ["pickup", "delivery-lagos", "quote"]
        },
        "lagosLga": { "type": "string" },
        "shippingAddress": { "type": "string" },
        "shippingCost": { "type": "number" },
        "status": {
          "type": "string",
          "enum": ["Pending Review", "Quote Ready", "Accepted", "Rejected", "Paid"]
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": ["userId", "customerName", "customerEmail", "items", "deliveryOption", "status", "createdAt"]
    },
    "Order": {
      "title": "Order",
      "description": "Represents a finalized and paid order.",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "quoteId": { "type": "string" },
        "customerName": { "type": "string" },
        "customerEmail": { "type": "string" },
        "items": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "name": { "type": "string" },
              "quantity": { "type": "string" },
              "unit": { "type": "string" },
              "unitCost": { "type": "number" }
            }
          }
        },
        "totalCost": { "type": "number" },
        "shippingAddress": { "type": "string" },
        "status": {
          "type": "string",
          "enum": ["Pending", "Processing", "Shipped", "Delivered", "Cancelled"]
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": ["userId", "quoteId", "totalCost", "status", "createdAt"]
    },
    "Notification": {
      "title": "Notification",
      "description": "Represents a notification for a user.",
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The ID of the user who this notification is for."
        },
        "title": { "type": "string" },
        "description": { "type": "string" },
        "href": { "type": "string" },
        "isRead": { "type": "boolean" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "title", "isRead", "createdAt"]
    },
    "StoreItem": {
        "title": "Store Item",
        "description": "Represents a single item in the store showcase.",
        "type": "object",
        "properties": {
            "name": {
                "type": "string",
                "description": "The name of the product."
            },
            "description": {
                "type": "string",
                "description": "A brief description of the product."
            },
            "imageUrl": {
                "type": "string",
                "format": "uri",
                "description": "The URL for the product image."
            }
        },
        "required": ["name", "description", "imageUrl"]
    },
    "StoreSettings": {
        "title": "Store Settings",
        "description": "Contains the settings for the product showcase.",
        "type": "object",
        "properties": {
            "items": {
                "type": "array",
                "description": "A list of products to display in the store.",
                "items": {
                    "$ref": "#/entities/StoreItem"
                }
            }
        }
    },
    "UnitOfMeasure": {
      "title": "Unit of Measure",
      "description": "A unit of measure for custom quote items.",
      "type": "object",
      "properties": {
        "name": { "type": "string" }
      },
      "required": ["name"]
    },
    "OptionalService": {
      "title": "Optional Service",
      "description": "An optional service for custom quotes.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "label": { "type": "string" }
      },
      "required": ["id", "label"]
    },
    "ShippingZone": {
      "title": "Shipping Zone",
      "description": "A shipping zone with a set fee.",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "fee": { "type": "number" }
      },
      "required": ["name", "fee"]
    },
    "CustomOrderSettings": {
      "title": "Custom Order Settings",
      "description": "Contains settings for the custom order form.",
      "type": "object",
      "properties": {
        "unitsOfMeasure": {
          "type": "array",
          "items": { "$ref": "#/entities/UnitOfMeasure" }
        },
        "optionalServices": {
          "type": "array",
          "items": { "$ref": "#/entities/OptionalService" }
        },
        "shippingZones": {
          "type": "array",
          "items": { "$ref": "#/entities/ShippingZone" }
        }
      }
    }
  },
  "auth": {
    "providers": ["email"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": { "$ref": "#/entities/UserProfile" },
      "description": "Stores public profile information for each user."
    },
    "/quotes/{quoteId}": {
      "schema": { "$ref": "#/entities/Quote" },
      "description": "Stores custom quote requests from users."
    },
    "/orders/{orderId}": {
      "schema": { "$ref": "#/entities/Order" },
      "description": "Stores finalized orders after payment."
    },
    "/users/{userId}/notifications/{notificationId}": {
      "schema": { "$ref": "#/entities/Notification" },
      "description": "Stores notifications for a specific user."
    },
    "/settings/store": {
        "schema": { "$ref": "#/entities/StoreSettings" },
        "description": "Stores the settings and product list for the store showcase. This is a single document."
    },
    "/settings/customOrder": {
      "schema": { "$ref": "#/entities/CustomOrderSettings" },
      "description": "Stores settings for the custom order form, such as units, services, and shipping zones. This is a single document."
    }
  }
}
